logging:
    level:
        no.skatteetaten.aurora.cantus: INFO
        org:
            hibernate: WARN
            springframework: WARN
            apache:
                catalina: WARN
                velocity: WARN

    pattern:
        console: "%d [%thread] %-5level %logger:%L %X - %msg %n%rEx{full}"

cantus:
    docker:
        url: "docker.com"
        urlsallowed: "docker.com,noBearerToken.com,allowedurl.no"
        internal:
            urls: "docker.com,vg.no"

